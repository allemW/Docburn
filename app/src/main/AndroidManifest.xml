<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codigopanda.nothotdog_tensorflow_classifier">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <application
        android:allowBackup="true"

        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Quemadura"></activity>
        <activity android:name=".Login"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Registrar"></activity>
        <activity android:name=".Tiposquemaduras"></activity>

        <activity android:name=".T_electricidad"></activity>

        <activity android:name=".T_fuego"></activity>

        <activity android:name=".T_liquido_caliente"></activity>

        <activity android:name=".T_solido_caliente"></activity>

        <activity android:name=".T_quimico"></activity>

    </application>

</manifest>